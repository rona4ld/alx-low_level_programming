#include <stdio.h>
/**
 * Descripition : main - prints out a string to stdout.
 * Returns: 0 if success.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
